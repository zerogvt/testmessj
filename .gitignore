# feature: exam-variants

node_modules/
dist/
.vite/
*.local

# Generated papers, if anyone ever writes them to disk while poking at the
# library. The sample sources themselves stay tracked.
student_*.docx
professor_*.docx

# Word lock files, left behind while a document is open
~$*.docx

.DS_Store
